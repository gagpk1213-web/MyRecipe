<Page x:Class="MyRecipes.Pages.PageAddEditIngredient"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MyRecipes.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PageAddEditIngredient">
    <Page.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor3}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor1}"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="{Binding WindowTitle}" FontSize="24" FontWeight="Bold"
                   Foreground="{StaticResource AccentColor1}" HorizontalAlignment="Center"
                   Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Название ингредиента -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Название ингредиента:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Ingredient.Name, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Цена -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Цена:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Ingredient.Cost, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="NumberValidationTextBox"/>
        </StackPanel>

        <!-- Количество для цены -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="Количество для цены:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Ingredient.CostForCount, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="NumberValidationTextBox"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,20,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Style="{StaticResource AccentButtonStyle}" Width="100" Margin="5"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource AccentButtonStyle}" Background="#6c757d" Width="100" Margin="5"/>
        </StackPanel>
    </Grid>
</Page>
