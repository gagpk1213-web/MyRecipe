<Page x:Class="MyRecipes.Pages.PageListIngredients"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:MyRecipes.Pages"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="1000"
      Title="PageListIngredients">
    <Page.Resources>
        <Style TargetType="Button" x:Key="PaginationButtonStyle">
            <Setter Property="Background" Value="{StaticResource AccentColor1}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor2}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Верхняя часть с суммарной информацией -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding TotalItemsText}" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource AccentColor1}" VerticalAlignment="Center" Grid.Column="0"/>
            <TextBlock Text="{Binding TotalCostText}" FontSize="16" FontWeight="Bold"
                       Foreground="{StaticResource AccentColor2}" VerticalAlignment="Center"
                       HorizontalAlignment="Right" Grid.Column="1"/>
        </Grid>

        <!-- Таблица ингредиентов -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding CurrentPageIngredients}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  BorderBrush="{StaticResource AccentColor3}" BorderThickness="1"
                  RowBackground="White" AlternatingRowBackground="#FFF8F8F8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                <DataGridTemplateColumn Header="Цена" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Cost, StringFormat={}{0:N2} руб.}" Foreground="{Binding PriceColor}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Кол-во для цены" Binding="{Binding CostForCount}" Width="120"/>
                <DataGridTextColumn Header="В наличии" Binding="{Binding AvailableCount}" Width="100"/>
                <DataGridTemplateColumn Header="Теги" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ItemsControl ItemsSource="{Binding Tags}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{Binding BackColor}" BorderBrush="Black" BorderThickness="1"
                                                CornerRadius="10" Padding="5,2" Margin="2">
                                            <TextBlock Text="{Binding Name}" Foreground="{Binding FontColor}" FontSize="10"/>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Действия" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Редактировать" ToolTip="Редактировать"
                                        Command="{Binding DataContext.EditIngredientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Margin="2"/>
                                <Button Content="Удалить" ToolTip="Удалить"
                                        Command="{Binding DataContext.DeleteIngredientCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                        CommandParameter="{Binding}" Margin="2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Нижняя часть с пагинацией -->
        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Пагинация -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="⏮" Command="{Binding FirstPageCommand}" Style="{StaticResource PaginationButtonStyle}"/>
                <Button Content="◀" Command="{Binding PreviousPageCommand}" Style="{StaticResource PaginationButtonStyle}"/>

                <ItemsControl ItemsSource="{Binding PageNumbers}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}" Command="{Binding DataContext.GoToPageCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}" Style="{StaticResource PaginationButtonStyle}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <Button Content=">" Command="{Binding NextPageCommand}" Style="{StaticResource PaginationButtonStyle}"/>
                <Button Content=">>" Command="{Binding LastPageCommand}" Style="{StaticResource PaginationButtonStyle}"/>
            </StackPanel>

            <!-- Кнопка добавления -->
            <Button Grid.Column="1" Content="Добавить ингредиент" Command="{Binding AddIngredientCommand}"
                    Style="{StaticResource AccentButtonStyle}" Width="150" Height="35"
                    Visibility="{Binding IsAdminVisibility}"/>
        </Grid>
    </Grid>
</Page>
