    <Application x:Class="MyRecipes.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MyRecipes">
    <Application.Resources>
        <SolidColorBrush x:Key="AccentColor1" Color="#2c3e50"/>
        <SolidColorBrush x:Key="AccentColor2" Color="#fdb249"/>
        <SolidColorBrush x:Key="AccentColor3" Color="#fed7c1"/>
        <SolidColorBrush x:Key="AccentColor4" Color="#eed4fe"/>

        <local:ImageConverter x:Key="ImageConverter"/>

        <Style TargetType="Button" x:Key="AccentButtonStyle">
            <Setter Property="Background" Value="{StaticResource AccentColor1}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor2}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
