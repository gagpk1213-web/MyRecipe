<Window x:Class="MyRecipes.Views.AddEditRecipeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyRecipes.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button" x:Key="AccentButtonStyle">
            <Setter Property="Background" Value="{StaticResource AccentColor1}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource AccentColor2}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentColor3}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentColor1}"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Название рецепта -->
        <StackPanel Grid.Row="0">
            <TextBlock Text="Название рецепта:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Recipe.Name, UpdateSourceTrigger=PropertyChanged}" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <!-- Описание -->
        <StackPanel Grid.Row="1">
            <TextBlock Text="Описание:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Recipe.Description, UpdateSourceTrigger=PropertyChanged}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Время приготовления и порций -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock Text="Время приготовления (мин):" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Recipe.CookingTime, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Количество порций:" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Recipe.Servings, UpdateSourceTrigger=PropertyChanged}" PreviewTextInput="NumberValidationTextBox"/>
            </StackPanel>
        </Grid>

        <!-- Инструкции -->
        <StackPanel Grid.Row="3">
            <TextBlock Text="Инструкции приготовления:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Recipe.Instructions, UpdateSourceTrigger=PropertyChanged}" Height="100" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Ингредиенты -->
        <StackPanel Grid.Row="4">
            <TextBlock Text="Ингредиенты:" Style="{StaticResource LabelStyle}"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button Content="Добавить ингредиент" Command="{Binding AddIngredientCommand}" Style="{StaticResource AccentButtonStyle}" Height="30"/>
                <Button Content="Удалить выбранный" Command="{Binding RemoveIngredientCommand}" Style="{StaticResource AccentButtonStyle}" Height="30" Background="#dc3545"/>
            </StackPanel>

            <ListView ItemsSource="{Binding Recipe.Ingredients}" SelectedItem="{Binding SelectedIngredient}"
                      Height="150" BorderBrush="{StaticResource AccentColor3}" BorderThickness="1">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Название" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Количество" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Quantity}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Единица измерения" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Unit}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Style="{StaticResource AccentButtonStyle}" IsDefault="True"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Style="{StaticResource AccentButtonStyle}" Background="#6c757d" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
